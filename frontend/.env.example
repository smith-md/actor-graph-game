# API URL - FastAPI backend for local dev, Cloudflare Workers for production
VITE_API_URL=http://localhost:8000

# Set to 'true' to use the edge API (Cloudflare Workers)
# When false or unset, uses the legacy FastAPI backend
VITE_USE_EDGE_API=false
